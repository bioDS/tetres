"""
Module contains Literals that are used for function input across the package.
"""
from typing import Literal

_DIST = Literal["rnni", "rf"]
_MDS_TYPES = Literal["tsne"]
_CLUSTERING_TYPE = Literal["spectral"]
